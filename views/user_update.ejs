<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/public/user_update.css" />
    <link
      href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"
      rel="stylesheet"
    />
    <link
      href="https://maxcdn.bootstrapcdn.com/font-awesome/latest/css/font-awesome.min.css"
      rel="stylesheet"
    />
    <title>Kullanıcı Güncelle</title>
  </head>
  <body>
    <div class="form-body">
      <div class="row">
        <div class="form-holder">
          <div class="form-content">
            <div class="form-items">
              <h3>Kullanıcı Güncelle</h3>
              <form
                class="requires-validation"
                action="/update_user"
                method="post"
              >
                <div class="col-md-12">
                  <input
                    class="form-control"
                    type="text"
                    name="id"
                    placeholder="ID"
                    value="<%= search_result[0]._id %>"
                    required
                  />
                </div>

                <div class="col-md-12">
                  <input
                    class="form-control"
                    type="text"
                    name="username"
                    placeholder="Kullanıcı Adı"
                    value="<%= search_result[0].username %>"
                    required
                  />
                </div>

                <div class="col-md-12">
                  <input
                    class="form-control"
                    type="password"
                    name="password"
                    placeholder="Password"
                    value="<%= search_result[0].password %>"
                    required
                  />
                </div>

                <div class="form-button mt-4">
                  <button id="submit" type="submit" class="btn btn-primary" style="margin-top: 10px; margin-left: 250px;" >
                    Güncelle
                  </button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!--   <form action="/update_user" method="post">
        <input type="text" name="id" readonly value="<%= search_result[0]._id %>">
        <input type="text" name="username" value="<%= search_result[0].username %>">
        <input type="text" name="password" value="<%= search_result[0].password %>">
        <button type="submit">Güncelle</button>
    </form>-->
  </body>
</html>
